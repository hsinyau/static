{
	"scripts": {
		"neodb:sync": "tsx scripts/neodb.ts",
		"neodb:get": "tsx -e \"import { getNeoDBData } from './scripts/neodb'; getNeoDBData().then(console.log).catch(console.error)\""
	},
	"dependencies": {
		"dotenv": "^17.0.1"
	},
	"devDependencies": {
		"@types/dotenv": "^8.2.3",
		"@types/node": "^24.0.10",
		"tsx": "^4.20.3"
	}
}
